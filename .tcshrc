# Allow tab completion.
set autoexpand
set autolist

# Set history size to 100 lines.
set history=100
# HISTFILE.
set savehist=100
set histfile=~/.tcsh_history

setenv EDITOR /opt/local/bin/vim
setenv PATH /opt/local/bin:$PATH
# set prompt=''
